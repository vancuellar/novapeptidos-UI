# Cualquier ruta que no exista como archivo se atiende con index.html y la resuelve el
# router de React, devolviendo 200 y no 404. Sin esto, /admin y todo lo que no esté
# pre-generado contestaba 404 — que es exactamente el fallo que un día sacó al sitio del
# índice de Google cuando vivía en GitHub Pages.
#
# ⚠️ AQUÍ SÓLO VAN RUTAS, NUNCA DOMINIOS COMPLETOS. Esa sintaxis (https://otro.com/*)
# es de Netlify; Cloudflare rechaza el archivo ENTERO si la encuentra, y entonces no
# funciona ni esta regla. Costó un despliegue descubrirlo (2026-07-28).
# Las redirecciones de exygenpeptides.com y compañía se hacen con reglas del panel.
/*  /index.html  200
